{
  "name": "ruvia-cosmatics-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix Frontend run build",
    "dev": "npm --prefix Frontend run dev",
    "start": "npm --prefix Frontend run start",
    "lint": "npm --prefix Frontend run lint",
    "install:all": "npm --prefix Frontend ci"
  },
  "comment": "This file exists so Vercel's Next.js framework detector can find a package.json at the repository root. Actual app code, dependencies, and build output live inside Frontend/. The vercel.json at the repo root overrides install/build commands to run inside Frontend/. Keep `next` listed below so Vercel's framework detector recognizes the project as Next.js.",
  "dependencies": {
    "next": "^16.2.6"
  }
}
